<%=
  form_for :relationships, routes.relationships_path do
    hidden_field :followed_id, value: user.id
    hidden_field :follower_id, value: current_user.id
    submit "Follow", class: "btn btn-primary"
  end
%>
