<h1>All users</h1>
<ul class="users">
  <% users.each do |user| %>
    <li>
      <%= gravatar_for user %>
      <%= link_to user.name, routes.user_path(user.id) %>
    </li>
  <% end %>
</ul>
<%= pagy_bootstrap_nav(pagy_data) %>
<%= pagy_bootstrap_nav_js(pagy_data) %>
