<%= link_to gravatar_for(current_user, size: 50), routes.user_path(current_user.id) %>
<h1><%= current_user.name %></h1>
<span><%= link_to "view my profile", routes.user_path(current_user.id) %></span>
<span><%= "#{user.microposts.count} microposts" %></span>
